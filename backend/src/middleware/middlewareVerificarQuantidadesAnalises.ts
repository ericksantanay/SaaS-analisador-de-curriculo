import { Request, Response, NextFunction } from "express";
import prisma from "../lib/prisma";

export function verificadorDeQuantidadeDeAnalises(req: Request, res: Response, next: NextFunction) {




};