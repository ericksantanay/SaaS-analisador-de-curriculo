ideia do middleware para monitorar a quantidade de analises ultilizadas, historico true ou false etc para ter o controle

# # # # # # # # # ## # # #  # # # # #  ## #
1. O Service (webhook.service.ts)
Recebe a notificação (o id do pagamento enviado pelo Mercado Pago).

Consulta a API do Mercado Pago para verificar se o status é approved.

Pega o external_reference (que é o userId que gravamos ao criar a assinatura).

Se estiver aprovado, faz o prisma.usuarios.update alterando o plano e liberando as análises.

2. O Controller (webhook.controller.ts)
Recebe a requisição do Mercado Pago.

Chama o Service do Webhook.

Responde HTTP 200 OK imediatamente para o Mercado Pago saber que você recebeu o aviso com sucesso (se você não responder 200, eles ficam tentando reenviar a notificação várias vezes).

3. A Rota (webhook.routes.ts)
Rota pública POST /webhook/mercadopago (ela não tem o middleware de autenticação verificarAutenticacao, pois quem chama essa rota é o robô do Mercado Pago, não o usuário logado).



# # # # # # # # # # # # # # # # # # #
Passo 1: Receber o Alerta: Seu Node.js recebe o aviso do Mercado Pago dizendo apenas o ID do pagamento (ex: 998877).

Passo 2: Investigar: Seu código pega esse ID e faz uma nova requisição (uma pergunta) para a API do Mercado Pago: "Mercado Pago, me dê todos os detalhes desse pagamento 998877, por favor".

Passo 3: A Descoberta: O Mercado Pago te devolve um JSON enorme com os detalhes do pagamento. E adivinha o que vem lá dentro intacto? A sua etiqueta! O external_reference.

Passo 4: Atualizar o Banco de Dados: Agora o seu sistema sabe o que fazer. Ele olha a etiqueta (external_reference), vê que é o ID 6a95cdc703fa... e usa o Prisma para ir no seu MongoDB fazer algo como: "Prisma, ache o usuário com esse ID e mude o status dele para PRO".
# # # # # # # # # # # # # # # # # # #



#
Ajustar a parte de validação do codigo, impedir que se o codigo nao estiver validado o usuario nao vai entrar
#


#
Na parte do codigo de verificação eu vou determinar uma quantidade de tentativas (4), se o usuario errar essa quantidade eu vou gerar codigo novo, 
#





# 
Fazer um controle no caso eu indentifico o tipo de plano que o usuario usou, e assim eu consigo definir os beneficios que ele vai ter e o que não vai ter
#


















#
O controle das paginas eu vou por no webhook, porque eu nao quero passar o plano quando o usuario faz a req, eu quero quando ele faz o pagamento!
#